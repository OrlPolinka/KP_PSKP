# ПРОСТАЯ НАСТРОЙКА DOCKER

## Проблема:
После моих исправлений вход в аккаунты перестал работать.

## Решение:
Вернул все к простой рабочей конфигурации:

### 1. Исправлен seed скрипт:
- Убраны поврежденные русские символы
- Исправлены email адреса (чтобы совпадали с теми, что были раньше)
- Упрощена структура (меньше пользователей, но все необходимое)

### 2. Упрощена Docker конфигурация:
- Удалены сложные скрипты
- Возвращена простая конфигурация как было

## Как запустить:

### Шаг 1: Пересобрать Docker
```bash
docker-compose down
docker-compose rm -f
docker-compose build --no-cache
docker-compose up -d
```

### Шаг 2: Проверить базу данных
```bash
# Запустить seed скрипт вручную
docker-compose exec backend node prisma/seed.js

# Проверить пользователей
docker-compose exec postgres psql -U postgres -d dance_studio -c "SELECT email, role FROM \"User\";"
```

### Шаг 3: Проверить вход
1. Откройте `http://localhost`
2. Войдите как:
   - **Админ**: `admin@dancestudio.ru` / `123456`
   - **Тренер**: `anna.zubareva@dancestudio.ru` / `123456`
   - **Клиент**: `maria.ivanova@mail.ru` / `123456`

## Тестовые пользователи:

### Админы:
- `admin@dancestudio.ru` / `123456`
- `admin2@dancestudio.ru` / `123456`

### Тренеры:
- `anna.zubareva@dancestudio.ru` / `123456` (Зумба, Дэнсхолл, Тверк)
- `dmitry.smirnov@dancestudio.ru` / `123456` (Хип-хоп, K-Pop, Хай-хилс)
- `ekaterina.ivanova@dancestudio.ru` / `123456` (Контемпорари, Стретчинг, Балет-фитнес)

### Клиенты:
- `maria.ivanova@mail.ru` / `123456`
- `alexey.petrov@mail.ru` / `123456`
- `olga.sidorova@mail.ru` / `123456`

## Что должно работать:

### ✅ Вход в аккаунты
- Все пароли: `123456`
- Все аккаунты активны

### ✅ Данные в базе:
- 8 пользователей
- 3 тренера с профилями
- 12 стилей танцев
- 5 залов
- 10 типов абонементов
- 35 занятий в расписании
- 3 активных абонемента

### ✅ Все страницы:
- Тренеры (3 тренера)
- Расписание (35 занятий)
- Абонементы (3 абонемента)
- Стили танцев (12 стилей)
- Личный кабинет (данные есть)

## Если все еще не работает:

### 1. Проверить логи:
```bash
docker-compose logs backend --tail=50
```

### 2. Проверить seed скрипт:
```bash
docker-compose exec backend node prisma/seed.js
```

### 3. Проверить миграции:
```bash
docker-compose exec backend npx prisma migrate deploy
```

### 4. Очистить кэш браузера:
- Нажмите **Ctrl+F5** в браузере
- Или откройте в режиме инкогнито

## Итог:
Вернул все к простой рабочей конфигурации как было раньше. Вход должен работать как прежде.